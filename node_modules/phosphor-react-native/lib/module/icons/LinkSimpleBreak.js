/* GENERATED FILE */
import React, { useContext, useMemo } from 'react';
import { IconContext } from '../lib';
import bold from '../bold/LinkSimpleBreak';
import duotone from '../duotone/LinkSimpleBreak';
import fill from '../fill/LinkSimpleBreak';
import light from '../light/LinkSimpleBreak';
import regular from '../regular/LinkSimpleBreak';
import thin from '../thin/LinkSimpleBreak';

function LinkSimpleBreak({
  weight,
  color,
  size,
  style,
  mirrored
}) {
  const {
    color: contextColor = '#000',
    size: contextSize = 24,
    weight: contextWeight = 'regular',
    mirrored: contextMirrored = false,
    style: contextStyle
  } = useContext(IconContext);
  const IconComponent = useMemo(() => {
    const iconWeight = weight !== null && weight !== void 0 ? weight : contextWeight;
    const weightMap = {
      bold,
      duotone,
      fill,
      light,
      regular,
      thin
    };
    return weightMap[iconWeight];
  }, [weight, contextWeight]);
  const mirroredValue = mirrored !== null && mirrored !== void 0 ? mirrored : contextMirrored;
  return /*#__PURE__*/React.createElement(IconComponent, {
    color: color !== null && color !== void 0 ? color : contextColor,
    size: size !== null && size !== void 0 ? size : contextSize,
    style: [contextStyle, style, { ...(mirroredValue && {
        transform: [{
          scaleX: -1
        }]
      })
    }]
  });
}

export default LinkSimpleBreak;
//# sourceMappingURL=LinkSimpleBreak.js.map